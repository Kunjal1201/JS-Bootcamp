//find the largest pandigital prime that exists
// eg 2143 is a 4-digit pandigit and is also prime

//convert an array into string
    const num = [2, 5, 3];
    const number = num.join('');
    console.log(number);

//convert an array into string
    const strNum = "123";
    const num1 = Number(strNum);
    console.log(num1);